<section id="home-main">
	<div class="container">
		<div class="row">
			<div class="col-md-4 col-md-offset-4 col-sm-6 col-sm-offset-3">
				<h2>I would like to...</h2>
				<div class="row">
					<div class="col-sm-6">
						<%= link_to venues_path, { :class => 'btn btn-lg btn-block btn-success' } do %><i class="fa fa-plus left"></i>Eat<% end %>
					</div>
					<div class="col-sm-6">
						<%= link_to venues_path, { :class => 'btn btn-lg btn-block btn-success' } do %><i class="fa fa-plus left"></i>Drink<% end %>
					</div>
				</div>
				<%= link_to new_venue_path, { :class => 'btn btn-lg btn-block btn-success' } do %><i class="fa fa-plus left"></i>Add a new Venue<% end %>
			</div>
		</div>
	</div>
</section>