<section id="home-main">
	<div class="container">
		<div class="row">
			<div class="col-md-8 col-md-offset-2 col-sm-10 col-sm-offset-1">
				<!--<%= form_tag venues_path, :method => 'get' do %>
					<h2 class="text-center">I would like...</h2>
					<%= text_field_tag :q, params[:q], { :class => 'form-control input-lg input-huge', :placeholder => 'mexican byob wicker park', :autofocus => true } %>
				<% end %>
				<hr>-->
				<div class="row">
					<div class="col-sm-6">
						<a href="/venuetypes/1" class="btn btn-lg btn-block btn-success"><i class="fa fa-cutlery left"></i>Restaurants</a>
					</div>
					<div class="col-sm-6">
						<a href="/venuetypes/2" class="btn btn-lg btn-block btn-success"><i class="fa fa-beer left"></i>Bars</a>
					</div>
				</div>
				<hr>
				<%= link_to new_venue_path, { :class => 'btn btn-lg btn-block btn-success' } do %><i class="fa fa-plus left"></i>Add a new Venue<% end %>
				<hr>
				<ul class="neighborhoods-list cols2">
					<% Neighborhood.all.sort{ |a,b| a.name <=> b.name }.each do |neighborhood| %>
					<li><%= link_to neighborhood_path(neighborhood) do %><%= neighborhood.name %><% end %></li>
					<% end %>
				</ul>
				<hr>
				<p class="text-center"><%= link_to 'Add Neighborhood', new_neighborhood_path %></p>
			</div>
		</div>
	</div>
</section>