<% provide(:title, "Edit #{@venue.name} | Venues") %>
<section id="venue-edit" class="pad">
	<div class="container">
		<div class="row">
			<div class="col-sm-12">
				<h1>Editing <%= @venue.name %></h1>
				<%= render :partial => 'form' %>
				<%= link_to @venue, title: 'Show', class: 'btn btn-primary' do %><i class="fa fa-undo left"></i>Show<% end %>&nbsp;<% if current_user.try(:admin?) %><%= link_to venue_path(@venue), title: 'Delete', method: :delete, data: { confirm: "Are you sure you would like to delete #{@venue.name}?" }, class: 'btn btn-danger' do %><i class="fa fa-trash-o left"></i>Delete<% end %><% else %><%= link_to deletion_request_path(:id => @venue.id, :type => 'venue'), title: 'Request Deletion', class: 'btn btn-danger' do %><i class="fa fa-trash-o left"></i>Request Deletion<% end %><% end %>
			</div>
		</div>
	</div>
</section>
